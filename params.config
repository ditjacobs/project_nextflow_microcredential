params { 
    // General Parameters
    datadir = "${launchDir}/data_test"
    outdir = "${launchDir}/results"
    // Input files
    vcfpath = "${params.datadir}/*.vcf"
    pheno = "${params.datadir}/{1,2}_*.csv"
}